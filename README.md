# Log-Analyzer
A Python tool for analyzing web server logs in Common Log Format (CLF) or JSON. Parses entries, filters by time, scores severity (LOW/MED/HIGH) for security insights (e.g., SQLi, errors, large POSTs), and generates summaries with top IPs, paths, statuses, and bytes.
